<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.smart4y.cloud.base.infrastructure.mapper.BaseAuthorityAppCustomMapper">
    <select id="selectAuthorityByApp" resultType="com.smart4y.cloud.core.dto.OpenAuthority">
        SELECT
            a.authority_id,
            a.authority,
            ap.expire_time,
            'app' AS owner
        FROM
            base_authority_app AS ap
        INNER JOIN base_authority AS a ON ap.authority_id = a.authority_id
        WHERE
            ap.app_id = #{appId}
        AND
            a.status = 1
    </select>
</mapper>